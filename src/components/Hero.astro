<div class="relative min-h-screen w-full flex items-center overflow-hidden">
  <!-- Content -->
  <div class="container mx-auto px-6 md:px-12 z-20 mt-20 lg:mt-0">
    <section class="max-w-4xl animate-slide-up">
      <h1
        class="text-6xl md:text-8xl lg:text-9xl font-bold font-tomaso leading-none mb-6 tracking-tighter text-white"
      >
        NUEVO <br />
        <span class="text-white drop-shadow-lg">EU5</span>
      </h1>

      <p
        class="text-xl md:text-3xl font-light text-white/90 max-w-2xl mb-10 leading-relaxed font-sans drop-shadow-md"
      >
        El sedán eléctrico que llegó a Colombia <span
          class="font-semibold text-[#EF4444]">para quedarse</span
        >.
      </p>

      <div class="flex flex-col sm:flex-row gap-4">
        <a
          href="#contact"
          class="inline-flex items-center justify-center px-8 py-4 bg-[#EF4444] hover:bg-[#DC2626] text-white font-bold rounded-full transition-all duration-300 transform hover:scale-105 hover:shadow-lg hover:shadow-red-500/30 group"
        >
          QUIERO MÁS INFORMACIÓN
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="h-5 w-5 ml-2 transition-transform group-hover:translate-x-1"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
          </svg>
        </a>
        <a
          href="#data"
          class="inline-flex items-center justify-center px-8 py-4 bg-white/10 hover:bg-white/20 backdrop-blur-md text-white font-semibold rounded-full border border-white/20 transition-all"
        >
          VER ESPECIFICACIONES
        </a>
      </div>
    </section>
  </div>

  <!-- Background Video -->
  <div class="absolute inset-0 z-10">
    <div
      class="absolute inset-0 bg-gradient-to-r from-black/60 via-black/30 to-transparent z-10"
    >
    </div>
    <video
      autoplay
      loop
      muted
      playsinline
      id="main-video"
      preload="auto"
      class="w-full h-full object-cover object-center scale-105"
    >
      <source src="/video.webm" type="video/webm" />
      <source src="/video.mp4" type="video/mp4" />
    </video>
  </div>
</div>

<style>
  @keyframes slideUp {
    0% {
      transform: translateY(30px);
      opacity: 0;
    }
    100% {
      transform: translateY(0);
      opacity: 1;
    }
  }
  .animate-slide-up {
    animation: slideUp 0.8s ease-out forwards;
  }
</style>
